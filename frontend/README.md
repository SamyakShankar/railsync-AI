# Frontend

**Owner:** frontend team.

**Must produce:** two independent React + Tailwind Vite view scaffolds: the schedule/Gantt view and the schematic corridor view. Shared frontend dependency declarations live in `package.json`.

**Do not touch outside this folder:** no backend routes, solver code, data fixtures, or integration code.

**Depends on:** backend endpoint and Task/schedule response contracts in `CONTRACTS.md`.

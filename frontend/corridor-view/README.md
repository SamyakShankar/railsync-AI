# Corridor View

**Owner:** frontend corridor-view team.

**Must produce:** a Vite + React + Tailwind entry scaffold for the future simplified SVG/React corridor screen. No map or UI logic belongs here yet.

**Do not touch outside this folder:** no schedule-view code, backend routes, optimizer code, data fixtures, or integration code. Frontend-wide dependency declarations are owned by `frontend/package.json`.

**Depends on:** Task and schedule response shapes plus `POST /disrupt` and `GET /plan/current` contracts in `CONTRACTS.md`.

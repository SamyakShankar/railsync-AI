# Integration

**Owner:** integration team.

**Must produce:** wiring and smoke checks that connect backend API routes, optimizer calls, and the two frontend views after their modules are implemented.

**Do not touch outside this folder:** do not change backend behavior, solver constraints, data schemas, or frontend implementation. Coordinate contract changes through `CONTRACTS.md` only.

**Depends on:** public interfaces from `backend/`, `optimizer/`, `data/`, and both `frontend/` views as frozen in `CONTRACTS.md`.
